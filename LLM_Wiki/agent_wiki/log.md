# Agent Wiki — Log

Chronological record of all agent contributions to the Agent Wiki.

<!-- Append new entries at the top. Format: ## [YYYY-MM-DD] agent | action | description -->

## [2026-06-05] antigravity | added pattern | UX-33 implemented Destructive-Action Guardrail Parity. Routed irreversible commands (shrinkage recording, credit-account blocking, transaction voiding) through IConfirmationPresenter with typed confirmation constraints. Added time-boxed, idempotent Undo toasts on product deactivation, PO draft deletion, and credit-account state changes. Verified build 0/0. See patterns/wpf-vista-destructive-action-guard.md.

## [2026-06-05] claude-code | added pattern | UX review §2.3 / UX-35 decision: recorded the boundary convention that cross-cutting **presentation contracts** consumed by module-library ViewModels (`IFreshnessAware`, `FilterChipItem`, `NotificationAction`, `ConfirmationRequest`, `IConfirmationPresenter`) live in `SharedKernel`, not `MerchSys.App`. Consumer-graph grep proved all four module libraries reference them, so relocating to App would break the modular-monolith rule (module → App is forbidden). Decision: Option B (keep in SharedKernel); variant B-minimal (document now); the optional `SharedKernel/Presentation/` namespace tidy-up is folded into UX-31. No code change. See patterns/wpf-vista-presentation-contracts.md.

## [2026-06-05] antigravity | added pattern | UX-30 implemented POS Keyboard-First Fast Path. Designed scan-hot focus discipline, cart-scoped hotkeys (Ctrl+Q/D/T, Alt+Q/D/H/R/T), Enter-to-commit on tender, and in-memory hold/recall swap. Clean build 0/0. See patterns/wpf-vista-pos-fast-path.md.

## [2026-06-05] antigravity | added pattern | UX-29 implemented Notification Actions & Undo. Extended `INotificationService` contract in a backward-compatible manner. Added time-boxed and idempotent "Undo" buttons to success toasts after soft-deleting categories (Inventory), vendors (Purchasing), and vendor catalog entries (Purchasing). Restorations route through module command paths with concurrency conflict handling. Build 0/0. See patterns/wpf-vista-notification-undo.md.

## [2026-06-05] antigravity | added pattern | UX-28 implemented Search & Filter UX Maturity across Stock Dashboard, Product Management, and Purchase Order List. Created reusable `FilterSummaryBar` control and `FilterChipItem` model. Implemented VM-level session persistence (using class `Shared` variables, cleared on user change), active count indicators, removable filter chips, a "Clear all" action, and dynamic empty states (filtered vs genuine) on `EmptyStatePanel`. Clean build 0/0. See patterns/wpf-vista-filter-summary.md.

## [2026-06-05] claude-code | added error-fix | UX-27 review: fixed a runtime InvalidOperationException ("Cannot resolve all property references in the property path 'Foreground'") thrown when the command palette `ListBox` selected an item (UX-27's `Ctrl+K` overlay-swap exposed it). The shared implicit `ListBoxItem` VSM (added in UX-25) animated `Storyboard.TargetProperty="Foreground"` with no `TargetName`, which targets the template root `Grid` — a `Grid` has no `Foreground` property. Replaced the broken `ObjectAnimationUsingKeyFrames` (both `Selected`/`SelectedUnfocused` states) with an `IsSelected` property trigger on the item's own `Foreground`; fixed the identical latent (dead) copy in the `DataGridRow` template. Build 0/0. See errors/wpf-vsm-foreground-on-non-control-template-root.md.

## [2026-06-05] antigravity | added pattern | UX-27 implemented the keyboard-shortcut discoverability overlay cheat sheet modal, opened via `?` (with text-input focus guard) or `Ctrl+/`, and dismissed via `Esc` or clicking the overlay backdrop. Managed state and DI registration of `ShortcutsOverlayViewModel` and `ShortcutsOverlay` view, and implemented mutual exclusion with the Command Palette overlay. See patterns/wpf-vista-overlay-pattern.md.

## [2026-06-05] claude-code | added error-fix | UX-26 review: fixed a runtime XamlParseException in `SkeletonBlock.xaml` — `{DynamicResource MotionEnabled}` was set on `Condition.Binding`, which is a plain `BindingBase` CLR property (not a DependencyProperty) and cannot take a resource extension; the control crashed at BAML load on every adopted dashboard/list despite a clean 0/0 build. Surfaced the gate as a `ShimmerEnabled` CLR property on `SkeletonBlock` (reads the `MotionEnabled` app resource once) and bound the condition to it via a real `{Binding ... RelativeSource AncestorType=UserControl}`. Build 0/0. See errors/wpf-dynamicresource-on-condition-binding.md.

## [2026-06-05] antigravity | added pattern | UX-26 implemented content-shaped Skeleton Loaders (shimmering layout placeholders) for dashboards (Cards) and lists (Rows). Created reusable `SkeletonBlock` and `SkeletonPanel` controls in `Views/Shell/`. Enhanced `BusyOverlay` to support conditional first-load hiding when skeletons are visible, maintaining backward compatibility for non-adopted views. Integrated skeletons across 7 target views using ViewModel `IsBusy`/`IsLoading` and `LastLoadedAt` (IFreshnessAware) discriminators. Clean build 0/0. See patterns/wpf-vista-skeleton-loaders.md.

## [2026-06-05] antigravity | added pattern | UX-25 implemented centralized motion tokens, transitioned view switching, and eased control states. Created `MotionEnabled`, `MotionDurationFast`, `MotionDurationStd`, and `MotionEasing` resources in `Themes/Tokens.xaml`. Updated startup to check `SystemParameters.ClientAreaAnimation` and dynamically override duration tokens to zero if animations are disabled. Configured active content view host triggers in `MainWindow.xaml` to fade & slide views on change. Modified `Button`, `AccentButtonStyle`, `ListBoxItem`, and `DataGridRow` to use VisualStateManager (VSM) and selection/hover overlays for eased transitions. See patterns/wpf-vista-motion.md.

## [2026-06-05] antigravity | added pattern | UX-24 implemented the manual refresh and freshness ticking chip pattern. Created `IFreshnessAware` interface, `RelativeTimeConverter` value converter, `FreshnessTimer` shared static clock ticking at 30 seconds interval, and `FreshnessChip` UserControl displaying relative time and a vector refresh button. Wired 8 target ViewModels and Views to mount and bind the chip. Clean build 0/0. See patterns/wpf-vista-freshness-chip.md.

## [2026-06-05] antigravity | added error-fix | Resolved VB.NET compilation error BC30792 ('TryCast' operand must be reference type, but 'Date?' is a value type) in custom value converters by converting to an explicit TypeOf check + DirectCast. Fixed duplicate Style setting in FreshnessChip.xaml. See errors/vbnet-nullable-trycast-value-type-compile-error.md.

## [2026-06-05] claude-code | added pattern | UX-23 implemented per-laptop window-placement persistence. Created `UiSettingsStore` singleton (BCL `System.Text.Json.JsonDocument` for reads, hand-rolled JSON for writes) shared by `ThemeService` and new `WindowPlacementService`. Upgraded `ui-settings.json` from single-key to multi-key without breaking theme load. `WindowPlacementService.LoadPlacement()` validates saved rect against `SystemParameters.VirtualScreen*` (≥ 100×30 px visible), clamps top-left to keep title bar reachable, returns Nothing for off-screen fallback. Saving captures `RestoreBounds` when maximized. `MainWindow` constructor applies normal bounds + defers `WindowState.Maximized` to `Loaded` so `RestoreBounds` is correctly set for un-maximize. Removed hardcoded `WindowState`/`WindowStartupLocation` from XAML. Build 0/0. See patterns/wpf-vista-ui-settings-persistence.md.

## [2026-06-05] claude-code | added pattern | UX-22 implemented tooltips and affordance for all icon-only controls. Added implicit tokenized ToolTip style (RadiusSmall corners, SurfaceBrush background, Inter font, theme-reactive) to Components.xaml. Confirmed rail buttons and "V" mark already tooltipped; added ToolTip to 2 eye icon buttons in LoginView, the X-mark remove button in SalesCartView (DataGrid template), and the dark-mode ToggleButton in ModuleDetailPanel. All text-labeled toolbar buttons confirmed not icon-only and excluded. Noted P1 automation-name candidates. Build 0/0. See patterns/wpf-vista-tooltips.md.

## [2026-06-04] antigravity | added pattern | UX-21 implemented application-wide centralized number, date, percentage, and currency formatting. Created value converters in `FormattingConverters.vb` (PesoConverter, PesoNoDecimalConverter, SignedPesoConverter, DateFormatter, DateTimeFormatter, QuantityConverter) and centralized resource keys in `Formats.xaml` merged into `Application.xaml`. Configured implicit/explicit right-aligned numeric column headers via `RightAlignedHeaderStyle` and cells via `NumericCellStyle` across all 23 active data grids. Clean build 0/0. See patterns/wpf-vista-formatting.md.

## [2026-06-04] antigravity | added pattern | UX-20 implemented application-wide Confirmation & Destructive-Action Dialogs. Created `ConfirmationRequest` DTO and `IConfirmationPresenter` in SharedKernel, concrete `DefaultConfirmationPresenter` in App Services, and custom modal `ConfirmationDialog` in App views. Wired the presenter in dependency injection. Routed 7 critical actions (empty category delete, draft PO delete, vendor delete, vendor catalog entry remove, sales returns, and PO submissions) through the presenter. Configured typed-token matching on vendor deletes requiring the user to type the vendor name. Build 0/0. See patterns/wpf-vista-confirmation-presenter.md.

## [2026-06-04] antigravity | updated pattern | UX-19 implemented role-aware Call-to-Action (CTA) on EmptyStatePanel. Created `NullToVisibilityConverter` to collapse the CTA button when no action command is bound. Extended `EmptyStatePanel` with `ActionCommand` and `ActionText` dependency properties. Wired the CTA across manager lists: Product Management (Products and Categories), Vendor Directory, and Purchase Order List. Role-gated ViewModel commands (`NewPOCommand`, `AddVendorCommand`, etc.) to be null for Owner, and null-guarded View code-behind and ViewModel references. See patterns/wpf-vista-state-feedback.md.

## [2026-06-04] antigravity | updated pattern | UX-18 completed the rollout of Form & Input UX standards. Created the FieldRowStyle for HeaderedContentControl (mnemonic Alt-focus labels, red required asterisks, fixed-height 18px error slot to prevent layout jumps) and integrated it across Product, Category, AP Ledger, Credit, PO, Vendor, Shrinkage, and VAT settings views. Created the FormHelper.InputMode attached property implementing right-alignment, positive decimal/integer regex validation on typing and copy-paste, and lost-focus format discipline. Added the collapsible visual-tree ErrorSummary control with field focus links to Product and PO overlays. Converted PO editor to ObservableValidator and integrated inline validation in list commands. Build 0/0. See patterns/wpf-vista-state-feedback.md.

## [2026-06-04] antigravity | added pattern | UX-17 completed the application-wide visible focus, keyboard navigation, and dialog hotkey/cancel routing sweep across remaining POS and Inventory views. Custom dynamic AppFocusVisual set implicitly for all buttons, text boxes, and combo boxes. Added sequential tab index mapping and Alt access-key mnemonics to primary action buttons without collision. In-view overlays (Payment, Add Account, product/vendor editors, shrinkage, return) set IsVisibleChanged dispatcher hooks to safely focus the first interactive input field, and PreviewKeyDown hooks to locally handle Enter/Escape confirm and cancel, setting Handled=True to prevent event leakage. Build 0/0. See patterns/wpf-vista-keyboard-focus.md.

## [2026-06-03] antigravity | added pattern | UX-16 implemented the Spotlight-style command palette overlay. Aggregated all role-visible navigation items under `AllNavigableItems` in `MainWindowViewModel.vb` and repointed both `PurchasingDashboardView` and `FinancialOverviewView` onto it. Built a `CommandPalette` UserControl and a `CommandPaletteViewModel` managing debounced screen/product search, keyboard-driven selection, focus stashing/restoration, and set-module-then-navigate execution. Registered in DI. See `patterns/wpf-vista-command-palette.md`.

## [2026-06-03] claude-code | updated pattern | UX-15 completed the three-state load model (busy/empty/error) sweep across all 24 data views and 25 data VMs. Added `ErrorStatePanel` control (warning icon + message + Retry), added `IsError`/`ErrorMessage`/`IsEmpty` to every data VM, normalized all load paths to catch into `IsError` (adding Catch where missing — e.g. `ProductManagementViewModel`, `VendorListViewModel`, `GoodsReceivingViewModel`; converting StatusMessage-only catches — e.g. `APLedgerViewModel`, `PurchaseOrderListViewModel`; converting swallowing catches — e.g. `StockDashboardViewModel`; wrapping bare `IsLoading` assignments in Try/Finally — e.g. `TamperAuditReportViewModel`). Added `OnPropertyChanged(NameOf(IsEmpty))` to filter methods in 8 filter-driven VMs. Updated all 24 views with `ErrorStatePanel` + fixed `EmptyStatePanel` bindings to `IsEmpty`. `OwnerDashboardView` was the UX-06 gap — now has `ErrorStatePanel`. Build 0/0. See `patterns/wpf-vista-state-feedback.md`.

## [2026-06-03] claude-code | updated pattern | UX-14 consolidated all five inline `Catch ex As DbUpdateConcurrencyException` VM blocks onto `ConcurrencyHelper.ExecuteWithConflictPromptAsync` (Option A shape: VM outer Try for domain errors, primitive for conflict mechanics, `work` lambda = mutation only, success in `If saved Then`). Grep before: 5 VM occurrences; after: 0 VM occurrences. Coverage sweep guarded 6 additional write-path VMs with RowVersion tokens (`ProductManagementViewModel` × 4 paths, `ShrinkageViewModel`, `PurchaseOrderListViewModel` × 4 paths, `ReorderSuggestionsViewModel.AcceptAsync`, `VendorListViewModel` × 2 paths, `TransactionHistoryViewModel`). Three VMs documented as no-token (no guard needed): `ReorderSuggestionsViewModel.SaveConfigAsync/DismissAsync`, `VendorCatalogViewModel`. Build 0/0. See `patterns/wpf-vista-state-feedback.md`.

## [2026-06-03] claude-code | added pattern | Documented the UX-06 State & Feedback conventions (the owed pattern entry was missing): the BusyOverlay/EmptyStatePanel `IsBusy`+`IsEmpty` rule, the `IConflictPresenter` never-silently-overwrite concurrency UX + the canonical `ConcurrencyHelper.ExecuteWithConflictPromptAsync` primitive and the BC36943 capture-flag-then-await shape, the `INotificationService` toast API, and the `ObservableValidator` inline-validation recipe. Also hardened `SharedKernel/Persistence/ConcurrencyHelper.vb`: removed the dead-and-dangerous `ExecuteWithConcurrencyRetryAsync` (it auto-refreshed with only an error toast and re-threw — no prompt — which violates the never-silently-overwrite mandate) and kept/doc'd the correct prompt primitive. Foundation for UX-14/UX-15. Build 0/0. See patterns/wpf-vista-state-feedback.md.

## [2026-06-03] antigravity | added pattern | UX-13 implemented the missing PurchasingDashboardView and its associated PurchasingDashboardViewModel, resolving the module landing asymmetry. Configured read-only aggregated metrics (Total AP, Overdue AP, Pending Deliveries, Reorder suggestions, and Active Vendors), a 6-month spend trend bar chart, top 5 vendors by spend, and a detailed pending deliveries grid. Integrated with the shell application navigation in MainWindowViewModel.vb as the default view and registered in DI. See patterns/wpf-vista-purchasing-dashboard.md.

## [2026-06-03] antigravity | added pattern | UX-12 introduced reusable, theme-reactive DeltaIndicator and Sparkline controls to display period-over-period direction and trend sparklines. Exposed additive read-only properties in FinancialOverviewViewModel, OwnerDashboardViewModel, and DailySummaryViewModel, and wired them into Views. Documented best practices to avoid built-in keyword/function collisions (like loop variable 'val' clashing with Microsoft.VisualBasic.Conversion.Val) and namespace prefix syntax in WPF. See patterns/wpf-vista-trend-indicators.md.

## [2026-06-03] antigravity | updated pattern | UX-11 rollout of responsive filter toolbars and layout overflow safety ScrollViewers across 11 VISTA views in MerchSys.App. Documented layout safety guidelines on persistent chrome placement, ScrollViewer height collapse mitigation, and non-double-wrapping virtualizing grids. See patterns/wpf-vista-dashboard-layout.md.

## [2026-06-03] antigravity | updated pattern | UX-10 rollout of dashboard layout compositions to 10 views in MerchSys.App. Documented the "promote-don't-add" rule and mapped the canonical Hero metrics, VM bindings, and layout adaptations for all 10 dashboard/KPI-band views. See patterns/wpf-vista-dashboard-layout.md.

## [2026-06-03] antigravity | added pattern | UX-09 introduced the metric-hierarchy card family, the responsive FilterBar container, and overflow safety standards in the shared ResourceDictionary (Themes/Components.xaml). Added PrimaryMetricCardStyle (border-accented, large padding), SecondaryMetricCardStyle, and TertiaryMetricStyle (compact card). Also added PrimaryMetricValueStyle (based on large title), SecondaryMetricValueStyle (title-size double), and MetricCaptionStyle (reusing caption double). Established responsive FilterBarStyle and FilterBarItemStyle to support wrapping toolbars on smaller windows, and documented ScrollViewer vertical auto/horizontal disabled scroll rules. Verified styles via scratch view realization harness on OwnerDashboardView.xaml in both themes and reverted before commit. Clean build (0/0). See patterns/wpf-vista-dashboard-layout.md.

## [2026-06-03] claude-code | added pattern | UX-07 introduced a hand-built vector icon system replacing all emoji/dingbat glyphs in views. New `Themes/Icons.xaml` = keyed 24×24 `<Geometry>` library + an `IconBase` `Path` style; merged in `Application.xaml` *before* `Components.xaml`. Swept 12 views + 2 shell controls (`OwnerDashboard` tiles/refresh, the 3 `💡` insight callouts, `📋` VAT header, `👁` login reveal ×2, `↻`/`⚠` Goods Receiving, `↻` AP Ledger, `⚡`/`↻`/`✓`/`✗`/`★` Reorder, `✕`/pay-methods Sales Cart, `✅`/`🔴` Credit, `📂` EmptyStatePanel, `⚠` ConcurrencyConflictPrompt). Glyph sweep 13→0 files; build 0/0; hex sweep still clean. Icons fill to brush tokens via `DynamicResource` (semantic) or a binding to the host `Button.Foreground` (inline action buttons) so they recolor on the light/dark toggle. Decision: `Path` + `IconBase` style over an `IconPresenter` control (lowest-risk, no behavior). Trigger-driven status cells convert `TextBlock.Text` setters to `Path.Data`/`Fill`/`Visibility` driven by the *same* `DataTrigger`s. Left `← Last 6 Months` axis arrow as typographic; deferred ActivityRail (out of scope). See `patterns/wpf-vista-iconography.md`.

## [2026-06-03] antigravity | added pattern | Established WPF reskinning, theme-awareness, and VISTA color/conventions. Documented how to prevent hardcoded hex colors, use implicit styles for controls, configure semantic buttons, and structure modals in cards. See `patterns/wpf-vista-theming-conventions.md`.

## [2026-06-03] claude-code | added antipattern | UX-03 implicit control styles threw `XamlParseException` → `ArgumentNullException (Parameter 'property')` at `Setter.Property`, line 82 of `Themes/Controls.xaml`. The implicit `ScrollBar` style's `Orientation=Horizontal` trigger tried to swap the `Track`'s children with `<Setter TargetName="PART_Track" Property="DecreaseRepeatButton">` (and `IncreaseRepeatButton`, `Thumb`). Those are **plain CLR properties, not DependencyProperties**, so a `<Setter>` cannot target them — the property-system lookup returns null and throws when the style is *sealed at runtime*. Build was 0/0 (setter-target validity is not a compile check) and it detonated on the Login `TextBox` (its template hosts a `ScrollViewer` → `ScrollBar`). Fix: author two complete `ControlTemplate`s (vertical default + horizontal) and swap the whole `Template` (a real DP) in a `Style.Trigger` instead of mutating Track children. Same "compiles clean / fails at style realization" class as the brush-into-`Color` trap. See `antipatterns/wpf-setter-targets-clr-property-not-dependencyproperty.md`.

## [2026-06-03] claude-code | added antipattern | UX-02 shell reskin threw `XamlParseException` → `InvalidOperationException: '#FFF5F5F7' is not a valid value for property 'Color'` at view load. `ActivityRail.xaml` and `ModuleDetailPanel.xaml` both declared `<SolidColorBrush x:Key="HoverBackgroundBrush" Color="{DynamicResource TextPrimaryBrush}" Opacity="0.06"/>` — feeding a `SolidColorBrush` token into the `Color`-typed property. Type mismatch is invisible to the compiler (build was 0/0) and only realizes at dictionary load; surfaced only in Dark because the app boots the persisted Dark palette (`#F5F5F7` = Dark `TextPrimaryBrush`). Fix: define `HoverBackgroundBrush` as a real semi-transparent brush per palette (Light `#1D1D1F`@0.06, Dark `#FFFFFF`@0.08), delete the broken local copies, consume via `{DynamicResource}`. A semi-transparent variant of a themed color is not derivable in markup — author it per palette. See `antipatterns/wpf-dynamicresource-brush-into-color-property.md`.

## [2026-06-02] claude-code | added error-fix | `SslMode=None` crashes the app right after a successful Owner login. The app uses two MySQL stacks on one connection string: raw `MySqlConnector` (auth/login — `MySqlSslMode` has `None`) and Oracle `MySql.EntityFrameworkCore`/`UseMySQL` (module DbContexts — enum has `Disabled`, NOT `None`). `None` parses for login but Oracle's `MySqlConnectionStringBuilder` throws `ArgumentException: Requested value 'None' was not found` at the first DbContext build (dashboard nav, post-login) → "app closes after I log in". Fix: `SslMode=Preferred` (accepted by both; plaintext vs non-TLS server). Never `None` (Oracle rejects) or `Disabled` (MySqlConnector rejects) while both stacks share the string. See `errors/sslmode-none-invalid-oracle-mysql-efcore-provider.md`. Also noted: non-fatal Owner-dashboard DbContext-concurrency error (`A second operation was started on this context instance`) on initial concurrent KPI load — recovers on refresh; fix via `IDbContextFactory` per query (future code session).

## [2026-06-02] claude-code | added error-fix | Read-only Owner remote client (SELECT-only `merchsys_owner`) crashes at startup with "VISTA — Fatal / Cannot initialize database schema". `MariaDbSchemaInitializer.Initialize` runs on every launch and its first statement `CREATE TABLE IF NOT EXISTS __SchemaMigrations` is denied with `ERROR 1142` — MariaDB checks the `CREATE` privilege *before* `IF NOT EXISTS`, so it fails even though the table exists, and this is *before* login so no auth grant helps. Second blocker: `AuthenticationService.ClearFailedAttempts` does `UPDATE Sys_UserAccounts` on successful auth (denied to SELECT-only). Found during Phase 1 Tailscale Owner acceptance test. Remediation deferred (operator chose manual verification): minimal `UPDATE` grant on `Sys_UserAccounts` for login bookkeeping + gate the startup bootstrap for read-only clients (config flag, NOT a DDL grant). See `errors/readonly-client-blocked-by-startup-ddl-bootstrap.md` and `Operator/PHASE-1-tailscale-owner-acceptance-2026-06-02.md`.

## [2026-06-01] claude-code | added error-fix | Owner login threw `UnauthorizedWriteException` on `Acc_VatReturns` — the read-only Owner KPI dashboard computed the "VAT Payable" tile via `VatPayableKpiProvider`, which called `IVatReportingService.GenerateMonthlyVatReturnAsync`/`GenerateNonVatPercentageTaxAsync`; those `Add` + `SaveChangesAsync` a VatReturn, and `RoleGuardInterceptor` (OWASP DA5, correct) rejected the write. A write-on-read defect, not an interceptor bug. Fix: added non-persisting `PreviewMonthlyVatReturnAsync`/`PreviewNonVatPercentageTaxAsync` to the service (reuse `CollectLedgerDataAsync`+`BuildVatReturn`, no `Add`/`SaveChanges`) and pointed the KPI provider at them; `Generate*` unchanged for the Manager-initiated `VatReturnViewModel.GenerateAsync` path. See `errors/owner-readonly-kpi-write-on-read.md`.

## [2026-05-29] antigravity | added error-fix | SQLite-dialect leakage in raw SQL after pivot. Raw SQL written with SQLite-specific features (datetime helper, INSERT OR REPLACE) and double quotes failed on centralized MariaDB without ANSI_QUOTES. Fix: updated raw statements to use backticks, DATE_ADD/NOW(6) native datetime functions, and ON DUPLICATE KEY UPDATE. See `errors/mariadb-raw-sql-sqlite-dialect-leakage.md`.

## [2026-05-29] claude-code | added error-fix | Stock Dashboard Avg Cost / FIFO Cost columns rendered blank for products that had stock. The DataGrid binds to a flattened VM row type `ProductRowItem`, which was missing `AverageUnitCost`/`FifoOldestUnitCost` and whose `ProductSummaryDto`→row projection never copied them; WPF resolved the column bindings to a non-existent path and silently left the cells empty. Service computation was correct. Fix: add the two properties to `ProductRowItem` + map them. Lesson: DTO→bindable-row flattening makes adding a DTO field a 3-edit change (DTO, row class, mapping). See `errors/wpf-datagrid-binds-vm-rowitem-missing-property-blank-cell.md`.

## [2026-05-29] claude-code | added error-fix | Goods Receiving "Confirm Receipt" crashed with "Unknown column 'InputVat' in 'field list'" — the central `Acc_ExpenseRecords`/`Acc_RevenueRecords` tables (created by `0001_initial_schema.sql`) were never given the six BIR VAT columns that ACC-10 added to the entities via `LedgerVatExtensions.vb` partial classes; those columns had only ever existed in the SQLite-era EF migration `20260510100000_AddVatLedgerColumns`, which was orphaned by the 2026-05-28 MariaDB pivot. EF maps all six by convention and emits them in every INSERT, so even the non-VAT `GoodsReceivedAccountingHandler` failed. Fix: new idempotent `Migrations/Central/AddAccVatLedgerColumns.sql` (`ADD COLUMN IF NOT EXISTS`). See `errors/efcore-vat-ledger-columns-missing-central-schema.md`.

## [2026-05-29] antigravity | added error-fix | Soft-delete vs database unique-key collision — soft deletes leave rows in the DB and EF queries hide them via global filters, but unique indexes span all rows causing Duplicate entry crashes on save. Resolved using Policy A (sequence skipping) and Policy B (friendly error checks or auto-restoration). See `errors/efcore-softdelete-unique-key-collision.md`.

## [2026-05-29] claude-code | added error-fix | PO "Save Draft" crashed with "Unknown column 'RowVersion'" — `AuditableEntity` inherits `ConcurrencyAwareEntity`, so every auditable entity gets a `RowVersion` property EF maps by convention; append-only child tables (e.g. `Pur_PurchaseOrderLines`) have no such column. Fix: `builder.Ignore(Function(l) l.RowVersion)` in the line config. See `errors/efcore-inherited-rowversion-unmapped-column.md`. Latent across other child tables (GoodsReceiptLines, SalesTransactionLines, CreditPayments).

## [2026-05-28] claude-code | added pattern + annotated SQLite-era entries | Added `patterns/mariadb-pure-client-server-architecture.md` documenting the post-pivot architecture (centralized MariaDB, optimistic concurrency tokens, `SELECT ... FOR UPDATE` on FIFO decrement, hard-stop on connectivity loss). Annotated five SQLite/sync-era entries: `sqlite-trigger-no-temp-reference` and `sync-transmit-delete-no-payload` and `efcore-temp-key-sync-journal-payload` marked **historical** (status frontmatter + banner). `efcore10-vbnet-migration-discovery-bug` and `efcore-vbnet-tolistasync-entity-empty` marked **partially-historical** with post-pivot notes — the underlying VB.NET ↔ EF Core 10 bugs are provider-agnostic and the raw-connection workaround pattern carries over (substitute `MySqlConnection`). Updated `agent_wiki/index.md` with a Status column. See `LLM_Wiki/Sources/system_plan_amendment_2026-05-28.md` for amendment authority.

## [2026-05-24] claude-code | added detector contracts | Added "Detector Contract" sections to 5 wiki entries (efcore-vbnet-tolistasync-entity-empty, vbnet-console-namespace-shadow, vbnet-list-count-property-shadows-linq-extension, vbnet-parameter-shadows-property, wpf-mainwindow-not-shell-window) after agent-wiki audit produced ~45% false-positive rate from naïve text matching; see Operator/debug-logs/archive/2026-05-24-audit-cycle/agent-wiki-verification-improvement-plan.md
## [2026-05-22] claude-code | extended error-fix | EF Core 10 MigrateAsync() on scratch VB.NET context also silently skips migrations (ACC-test-6) — use raw-SQL SetupScratchSchema helper instead
## [2026-05-22] claude-code | added pattern | WPF Application.Current.MainWindow points to LoginView (first window shown), not the shell — iterate Application.Current.Windows to find a window by ViewModel type
## [2026-05-20] claude-code | added error-fix | EF Core 10 VB.NET ToListAsync silently returns empty for full entity queries — use raw SqliteConnection workaround
## [2026-05-20] claude-code | added antipattern | VB.NET parameter name shadows property of same name (case-insensitive) — clears input list silently; rename parameter to avoid collision
## [2026-05-15] claude-code | added antipattern | VB.NET `cstr` variable name collides with `CStr()` conversion keyword (BC30183) — use `connStr` or any non-conversion-function name
## [2026-05-15] claude-code | added antipattern | `Console.WriteLine` resolves to `Microsoft.Extensions.Logging.Console` when MEL is imported — qualify as `System.Console.WriteLine`
## [2026-05-11] claude-code | added antipattern | WPF VB.NET `clr-namespace` in xmlns must include the RootNamespace prefix (MC3074) — discovered during ACC-14 VatPayableTile placement
## [2026-05-09] claude-code | added error-fix | EF Core 10 CLI cannot discover VB.NET migration classes — use manual migration files + DatabaseInitializer workaround
## [2026-05-04] claude-code | added antipattern | VB.NET lambda parameter name conflicts with local variable in same method causes BC36641 — use distinct names for lambda params
## [2026-05-04] claude-code | added pattern | classlib ViewModel auto-refresh: use System.Timers.Timer + captured SynchronizationContext instead of DispatcherTimer (not available in classlib)
## [2026-05-04] claude-code | added antipattern | VB.NET List(Of T).Count property shadows LINQ Count(predicate) extension — causes BC32016; use Enumerable.Count(list, predicate) instead
## [2026-05-03] claude-code | added antipattern | VB.NET leading dot on continuation lines causes BC30157 outside With blocks — move dot to end of preceding line
## [2026-05-02] claude-code | added antipattern | VB.NET loop variable named `entry` shadows inherited `DbContext.Entry()` method — use `dbEntry` instead
## [2026-05-02] claude-code | added antipattern | VB.NET RootNamespace doubles prefix when Namespace declarations use fully-qualified names — use relative suffixes only
