<UserControl x:Class="Views.Shell.Modules.InventoryPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="220">
    <StackPanel>
        <ItemsControl ItemsSource="{Binding InventoryItems}" Focusable="False">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                     <Button Content="{Binding DisplayName}"
                            Style="{DynamicResource NavItemStyle}"
                            Command="{Binding DataContext.NavigateCommand,
                                               RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
