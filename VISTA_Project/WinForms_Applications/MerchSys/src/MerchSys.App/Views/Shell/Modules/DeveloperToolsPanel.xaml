<UserControl x:Class="Views.Shell.Modules.DeveloperToolsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="220">
    <StackPanel>
        <!-- Debug-build only: shows developer tool shortcuts -->
        <ItemsControl ItemsSource="{Binding DeveloperToolsItems}" Focusable="False">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Button Content="{Binding DisplayName}"
                                Style="{DynamicResource NavItemStyle}"
                                Command="{Binding DataContext.NavigateCommand,
                                                   RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"/>
                        <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                                Margin="0,0,4,0"
                                Style="{DynamicResource FavoriteToggleButtonStyle}"
                                Command="{Binding DataContext.ToggleFavoriteCommand,
                                                   RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                AutomationProperties.Name="{Binding DisplayName, StringFormat='Toggle favorite for {0}'}">
                            <TextBlock>
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="☆"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                <Setter Property="Text" Value="★"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
