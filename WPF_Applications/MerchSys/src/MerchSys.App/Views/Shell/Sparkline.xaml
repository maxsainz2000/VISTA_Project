<UserControl x:Class="Views.Shell.Sparkline"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="24" d:DesignWidth="60"
             Height="24">
    <ItemsControl ItemsSource="{Binding InternalBars, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border Margin="1,0" VerticalAlignment="Bottom">
                    <Rectangle Width="3" Height="{Binding Height}"
                               Fill="{DynamicResource AccentBrush}"
                               RadiusX="0.5" RadiusY="0.5">
                        <Rectangle.ToolTip>
                            <TextBlock Text="{Binding Value, StringFormat={StaticResource FormatCurrencyNoDecimal}}"/>
                        </Rectangle.ToolTip>
                    </Rectangle>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
