<Window x:Class="Views.Shell.ConcurrencyConflictPrompt"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Write Conflict" Height="180" Width="360"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        BorderBrush="{DynamicResource SeparatorBrush}" BorderThickness="1">
    
    <Grid Margin="20">
        <DockPanel>
            <!-- Header Icon & Text -->
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="⚠" FontSize="28" Foreground="{DynamicResource WarningBrush}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Data changed elsewhere — refresh and retry." FontWeight="Bold" FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}" TextWrapping="Wrap" Width="260"/>
                    <TextBlock Text="Another operator has modified this record. Your changes cannot be saved." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Width="260" Margin="0,4,0,0"/>
                </StackPanel>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Style="{StaticResource SecondaryButtonStyle}" Click="CancelButton_Click" IsCancel="True" Margin="0,0,10,0"/>
                <Button Content="Refresh" Style="{StaticResource PrimaryButtonStyle}" Click="RefreshButton_Click" IsDefault="True"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
