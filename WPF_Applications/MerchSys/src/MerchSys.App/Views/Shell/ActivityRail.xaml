<UserControl x:Class="Views.Shell.ActivityRail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MerchSys.App.ViewModels.Shell"
             xmlns:models="clr-namespace:MerchSys.App.Models"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="60"
             Background="#1A252F">

    <UserControl.Resources>

        <!-- Rail icon button — 60×60, centered label, left-accent when active -->
        <Style x:Key="RailButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <!-- Left accent bar — 3px, visible when active -->
                            <Border x:Name="AccentBar"
                                    Width="3"
                                    HorizontalAlignment="Left"
                                    Background="#2980B9"
                                    Visibility="Collapsed"/>
                            <!-- Background layer for hover -->
                            <Border x:Name="Bg"
                                    Background="Transparent"/>
                            <!-- Content -->
                            <ContentPresenter VerticalAlignment="Center"
                                              HorizontalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#2C3E50"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <DockPanel>

        <!-- App identifier at top -->
        <TextBlock DockPanel.Dock="Top"
                   Text="V"
                   Foreground="White"
                   FontSize="18"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,16,0,16"
                   ToolTip="VISTA — Villon Integrated Supply and Trade Application"/>

        <!-- Divider -->
        <Border DockPanel.Dock="Top" Height="1" Background="#2C3E50" Margin="4,0,4,4"/>

        <!-- Module icon buttons -->
        <ItemsControl DockPanel.Dock="Top"
                      ItemsSource="{Binding RailItems}"
                      Focusable="False">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type models:RailItem}">
                    <Button Command="{Binding DataContext.SelectModuleCommand,
                                              RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding ModuleId}"
                            ToolTip="{Binding ToolTipText}"
                            Content="{Binding Abbreviation}">
                        <!-- Style includes base rail chrome + IsActive trigger for accent bar -->
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource RailButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="Background" Value="#243342"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Grid>
                                                        <Border Background="#243342"/>
                                                        <Border Width="3"
                                                                HorizontalAlignment="Left"
                                                                Background="#2980B9"/>
                                                        <ContentPresenter VerticalAlignment="Center"
                                                                          HorizontalAlignment="Center"/>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </DockPanel>

</UserControl>
