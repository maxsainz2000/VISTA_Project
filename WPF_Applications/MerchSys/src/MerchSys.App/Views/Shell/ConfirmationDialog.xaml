<Window x:Class="Views.Shell.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:helpers="clr-namespace:MerchSys.App.Helpers"
        mc:Ignorable="d"
        Title="Confirm Action" Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        BorderBrush="{DynamicResource SeparatorBrush}" BorderThickness="1"
        helpers:AccessibilityHelper.IsFocusTrap="True">
    
    <Grid Margin="20">
        <DockPanel>
            <!-- Header Icon & Text -->
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,16">
                <Path x:Name="DialogIcon" Style="{StaticResource IconBase}" Data="{StaticResource IconWarningGeometry}"
                      Width="28" Height="28" Fill="{DynamicResource WarningBrush}" VerticalAlignment="Top" Margin="0,2,12,0"/>
                <StackPanel VerticalAlignment="Center" Width="350">
                    <TextBlock x:Name="TitleTextBlock" Text="Confirm Action" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextPrimaryBrush}" TextWrapping="Wrap"/>
                    <TextBlock x:Name="MessageTextBlock" Text="Are you sure you want to proceed?" FontSize="11.5" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,6,0,0" LineHeight="16"/>
                </StackPanel>
            </StackPanel>

            <!-- Typed Confirmation Area -->
            <StackPanel x:Name="TypedConfirmationArea" DockPanel.Dock="Top" Margin="40,0,0,16" Visibility="Collapsed">
                <TextBlock x:Name="InstructionTextBlock" Text="Please type the token to confirm:" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                <TextBox x:Name="ConfirmationTextBox" Width="220" Height="28" HorizontalAlignment="Left" Padding="6,2" FontSize="12" VerticalContentAlignment="Center" BorderBrush="{DynamicResource SeparatorBrush}" BorderThickness="1"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton" Content="_Cancel" TabIndex="2" Style="{StaticResource SecondaryButtonStyle}" Click="CancelButton_Click" IsCancel="True" Margin="0,0,10,0" Padding="14,6"/>
                <Button x:Name="ConfirmButton" Content="_Confirm" TabIndex="1" Style="{StaticResource PrimaryButtonStyle}" Click="ConfirmButton_Click" IsDefault="True" Padding="14,6"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
